dynamic ranks = DynamicRanks;

class Out {
  Out(ArgumentError argumentError, argument);
}

Type Output() => Argument;

class Argument {
  Argument(ArgumentError argumentError, argument);
}

class DynamicRanks {
  DynamicRanks(Type argumentError, argument);
}

void main() {
  Object();
}
